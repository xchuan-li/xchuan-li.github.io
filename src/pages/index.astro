---
import Base from "../layouts/Base.astro";
import { posts } from "../data/writing";
import { openingQuestionsHtml, identityLongHtml } from "../data/program";

// Homepage (A3g §5). Section order: header → current work → selected work →
// research program preview → writing → timeline → playground → contact. Research
// evidence comes before biography, so the timeline sits below the projects. The
// identity line comes from data/program.ts; page-specific copy lives here.
//
// PHOTO: public/images/headshot.jpg (CSS background — a missing file just shows an
// empty frame). LOGOS: public/images/logos/{utn,marburg,szu}.svg in .tl-logo;
// circular emblems (emblem: true) are sized up to match the UTN wordmark.

const selectedWork = [
  {
    title: "Three Ways of Leaving <em>p</em> Unsettled",
    href: "/research/three-ways",
    cover: "/images/project-covers/three-ways.png",
    coverAlt: "Three semantic states showing one-sided possibility, bilateral openness, and unresolved knowledge",
    current: true,
    desc: "This paper compares three ways of leaving a question unresolved: <em>might p</em>, <em>might p or might not p</em>, and <em>I don’t know whether p</em>. It asks how each utterance changes the discourse and which replies each makes appropriate. A construct brief is currently under review by a formal semanticist.",
  },
  {
    title: "Learning epistemic modality from linguistic input",
    href: "/research/learnability",
    cover: "/images/project-covers/learnability.png",
    coverAlt: "A corpus passing through controlled input interventions, a small learner, and generalization tests",
    desc: "The master’s thesis asks whether a small language model can learn these distinctions from developmentally realistic text. It begins with a formal analysis, then tests which patterns in the training input are needed for the distinctions to emerge.",
  },
  {
    title: "Cross-lingual prototypicality bias in vision–language models",
    href: "/research/cross-lingual-protobias",
    cover: "/images/project-covers/protobias.png",
    coverAlt: "Seven language prompts feeding one vision-language judge comparing a duck and panda benchmark image pair",
    desc: "Each image pair stays fixed while the prompt language varies, testing when vision-language judges follow semantic fit and when they fall back on culturally specific prototypes. The study contains about 12,600 judgments across seven languages and two model families. Code is public, and a revised manuscript is being prepared for arXiv.",
  },
  {
    title: "Prompt framing and latent control states in language models",
    href: "/research/latent-control-states",
    cover: "/images/project-covers/latent-control-states.png",
    coverAlt: "A utilitarian diversion dilemma passing through model layers with a causal intervention on an internal direction",
    desc: "In this collaboration with DFKI, we ask whether prompt framing changes only a model’s answer or also a causally relevant internal state. The design is a utilitarian minimal-pair experiment that varies the dilemma, benefit size, and framing.",
  },
];

const timeline = [
  {
    period: "2025 – present",
    logo: "/images/logos/utn.svg",
    org: "University of Technology Nuremberg (UTN)",
    role: "M.Sc. Human and Artificial Intelligence",
    did: "A move from philosophy into language-model research, learning to build controlled experiments that test what a model actually represents about meaning.",
    questions: [
      "Can a language model acquire a semantic distinction from text alone?",
      "Which patterns in the training input make it learnable?",
      "Does saying that something <em>might</em> be true simply lower commitment, or keep that possibility open for later reasoning?",
    ],
  },
  {
    period: "2024 – 2025",
    logo: "/images/logos/marburg.svg",
    emblem: true,
    org: "University of Marburg",
    role: "Student · Preparatory Studies",
    did: "A year of preparatory studies as an enrolled student, learning German for graduate work — and reading the same meanings through three grammars: German, Chinese, and English.",
    questions: [
      "When is one meaning carried by very different structures — case, word order, clause shape?",
      "How much of an inference survives when a sentence crosses languages?",
      "Do different languages leave the same things unsaid?",
    ],
  },
  {
    period: "2020 – 2024",
    logo: "/images/logos/szu.svg",
    emblem: true,
    org: "Shenzhen University",
    role: "B.A. Philosophy",
    did: "Philosophy of language, analytic philosophy, and logic — with a senior thesis on Leibniz’s <em>characteristica universalis</em>, a notation whose form was meant to mirror the structure of thought.",
    questions: [
      "How much of reasoning is carried by the language it is expressed in?",
      "Can meaning be given a form precise enough to compute with?",
      "What does a sentence settle, and what does it leave open?",
    ],
  },
];

const playground = [
  {
    variant: "relay",
    title: "RELAY",
    tag: "Language-puzzle game",
    href: "/relay/",
    cta: "Play the prototype",
    steps: ["Observe", "Infer", "Compose"],
    desc: "You’re handed a machine that speaks a language nobody taught you. Feed it inputs, watch how it answers, and reverse-engineer the rules — then compose a brand-new utterance and see whether it actually gets through.",
  },
  {
    variant: "im",
    title: "Information Machines",
    tag: "Constructed language",
    href: "/information-machines",
    cta: "Read the design log",
    steps: ["Add", "Link", "Revise", "Retract"],
    desc: "A language whose grammar has a single job: move information around — introduce a fact, link it to what came before, revise it, take it back. It grows slowly, kept honest with a changelog and a suite of tests.",
  },
];

const featured = posts.find((p) => p.featured) ?? posts[0];
---

<Base wide={true}>
  <div class="wrap">
    <!-- ===== header ===== -->
    <header class="hd">
      <div class="hd-main">
        <h1 class="hd-name">Xiaochuan Li</h1>
        <p class="hd-tag" set:html={openingQuestionsHtml} />
        <p class="hd-tag hd-tag-work" set:html={identityLongHtml} />
        <p class="hd-affil">
          M.Sc. Human and AI · University of Technology Nuremberg · applying for PhD positions beginning in fall 2027
        </p>
        <p class="hd-links">
          <a href="mailto:xiaochuan.li@utn.de">xiaochuan.li@utn.de</a>
          <span class="sep">·</span>
          <a href="/cv">CV</a>
          <span class="sep">·</span>
          <a href="https://github.com/xchuan-li" rel="noopener">GitHub</a>
        </p>
      </div>
      <div class="hd-photo" role="img" aria-label="Portrait of Xiaochuan Li"></div>
    </header>

    <!-- ===== current work ===== -->
    <section class="sec">
      <h2 class="sec-h">Current work</h2>
      <h3 class="blk-h">What does <em>might</em> leave open?</h3>
      <p class="blk-body">
        Suppose someone says, “The key might be in the drawer.” Does <em>might</em> simply mark
        uncertainty, or does it keep the drawer possibility available when new evidence arrives? The
        current work develops a semantic account of this difference and the diagnostics needed to
        test it.
      </p>
      <p class="blk-link"><a href="/research">Read about the current work</a></p>
    </section>

    <!-- ===== selected work ===== -->
    <section class="sec">
      <h2 class="sec-h">Selected work</h2>
      <div class="proj-list">
        {selectedWork.map((p) => (
          <a class={`proj-card${p.current ? " proj-card-current" : ""}`} href={p.href} aria-label={`Open ${p.title.replace(/<[^>]+>/g, "")}`}>
            <div class="proj-copy">
              <div class="proj-top">
                <span class="proj-title" set:html={p.title} />
              </div>
              <p class="proj-desc" set:html={p.desc} />
            </div>
            <div class="proj-figure">
              <img src={p.cover} alt={p.coverAlt} width="1200" height="720" loading="lazy" decoding="async" />
            </div>
          </a>
        ))}
      </div>
    </section>

    <!-- ===== research program preview ===== -->
    <section class="sec">
      <h2 class="sec-h">Research program</h2>
      <h3 class="blk-h">From linguistic analysis to evidence</h3>
      <p class="blk-body">
        The broader program connects formal semantics with two empirical questions. How does a
        linguistic function emerge in human cognition? Can it be learned from linguistic input alone?
        The same semantic analysis formulates both questions, without treating models as
        stand-ins for people.
      </p>
      <p class="blk-link"><a href="/approach">See the research program</a></p>
    </section>

    <!-- ===== writing ===== -->
    <section class="sec">
      <h2 class="sec-h">Writing</h2>
      {featured && (
        <a class="feat" href={`/writing/${featured.slug}`}>
          <span class="feat-title">{featured.title}</span>
          <span class="feat-excerpt">{featured.excerpt}</span>
        </a>
      )}
      <p class="more"><a href="/writing">All writing</a></p>
    </section>

    <!-- ===== timeline ===== -->
    <section class="sec">
      <h2 class="sec-h">Timeline</h2>
      <div class="tl">
        {timeline.map((t) => (
          <div class="tl-item">
            <div class="tl-aside">
              <div class={`tl-logo${t.emblem ? " emblem" : ""}`}>
                <img src={t.logo} alt={`${t.org} logo`} />
              </div>
              <div class="tl-period">{t.period}</div>
            </div>
            <div class="tl-body">
              <h3 class="tl-org">{t.org}</h3>
              <p class="tl-role">{t.role}</p>
              <p class="tl-did" set:html={t.did} />
              {t.questions && (
                <ul class="tl-qs">
                  {t.questions.map((q) => <li set:html={q} />)}
                </ul>
              )}
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- ===== playground ===== -->
    <section class="sec play-sec" id="playground">
      <h2 class="sec-h">Playground</h2>
      <p class="block-note play-note">
        Outside the research, I like building small, <em>playable</em> things out of language — a toy
        grammar with its own test suite, a puzzle where you reverse-engineer a language you’ve never
        seen. Same curiosity as the papers, with room to follow a hunch and see what it does.
      </p>
      <div class="play-grid">
        {playground.map((p) => (
          <a class={`play-card play-${p.variant}`} href={p.href} aria-label={`Open ${p.title}`}>
            <div class="play-fig" aria-hidden="true">
              {p.variant === "relay" && (
                <svg class="pr" viewBox="0 0 220 200" role="img" aria-label="The RELAY language machine">
                  <g class="pr-rings">
                    <circle class="pr-ring pr-ring-a" cx="110" cy="100" r="86" />
                    <circle class="pr-ring pr-ring-b" cx="110" cy="100" r="73" />
                  </g>
                  <circle class="pr-disc" cx="110" cy="100" r="66" />
                  <g class="pr-bot">
                    <path class="pr-stalk" d="M64 8v16" />
                    <circle class="pr-bulb" cx="64" cy="8" r="7" />
                    <path class="pr-ear" d="M22 49H12v34h10M106 49h10v34h-10" />
                    <rect class="pr-head" x="20" y="25" width="88" height="86" rx="24" />
                    <rect class="pr-face" x="34" y="42" width="60" height="52" rx="15" />
                    <g class="pr-eyes">
                      <rect class="pr-eye" x="44" y="57" width="16" height="12" />
                      <rect class="pr-eye" x="68" y="57" width="16" height="12" />
                      <rect class="pr-pupil" x="50" y="61" width="6" height="6" />
                      <rect class="pr-pupil" x="72" y="61" width="6" height="6" />
                    </g>
                    <path class="pr-mouth" d="M49 80h30" />
                  </g>
                </svg>
              )}
              {p.variant === "im" && (
                <svg class="pg" viewBox="0 0 220 200" role="img" aria-label="An information state being introduced, linked, and revised">
                  <line class="pg-edge" x1="66" y1="132" x2="156" y2="74" />
                  <circle class="pg-spark" cx="66" cy="132" r="4.5" />
                  <g class="pg-node pg-q">
                    <circle cx="156" cy="74" r="21" />
                    <text x="156" y="75" class="pg-label">q</text>
                  </g>
                  <g class="pg-node pg-p">
                    <circle cx="66" cy="132" r="21" />
                    <text x="66" y="133" class="pg-label">p</text>
                  </g>
                  <g class="pg-revise">
                    <circle class="pg-halo" cx="66" cy="132" r="27" />
                    <text x="95" y="120" class="pg-prime">′</text>
                  </g>
                </svg>
              )}
            </div>
            <div class="play-body">
              <div class="play-top">
                <span class="play-title">{p.title}</span>
                <span class="play-tag">{p.tag}</span>
              </div>
              <p class="play-desc">{p.desc}</p>
              <div class={`play-chips play-chips-${p.variant}`}>
                {p.steps.map((s) => <span class="play-chip">{s}</span>)}
              </div>
              <span class="play-cta">{p.cta}<span class="play-cta-arrow">→</span></span>
            </div>
          </a>
        ))}
      </div>
    </section>

    <!-- ===== contact ===== -->
    <section class="sec">
      <div class="cta">
        <p class="cta-line">Currently applying for PhD positions beginning in fall 2027 — email is the best way to get in touch.</p>
        <p class="cta-links">
          <a href="mailto:xiaochuan.li@utn.de">Email me</a>
          <span class="sep">·</span>
          <a href="/contact">More ways to get in touch</a>
        </p>
      </div>
    </section>
  </div>
</Base>

<style>
  .wrap { max-width: 940px; margin: 0 auto; padding: 2.75rem 1.5rem 5rem; font-family: var(--font-sans); }

  /* ===== header ===== */
  .hd { display: flex; align-items: center; justify-content: space-between; gap: 2.5rem; margin-bottom: 1rem; }
  .hd-photo {
    flex: none;
    width: 208px;
    height: 260px;
    border-radius: 14px;
    border: 1px solid var(--color-border);
    background-color: var(--color-surface-2);
    background-image: url("/images/headshot.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }
  .hd-main { min-width: 0; }
  .hd-name { font-family: var(--font-display); font-size: 2.5rem; font-weight: 600; letter-spacing: -0.02em; margin: 0 0 0.6rem; color: var(--color-text); line-height: 1.05; }
  .hd-tag { font-size: 1.15rem; line-height: 1.5; color: var(--color-text); margin: 0 0 0.7rem; max-width: 34rem; }
  .hd-tag :global(strong) { font-weight: 600; }
  .hd-tag :global(em) { font-style: italic; }
  .hd-tag-work { font-size: 1.02rem; color: var(--color-text-muted); margin: 0 0 0.9rem; }
  .hd-tag-work :global(em) { font-style: italic; color: var(--color-text); }
  .hd-affil { font-size: 0.9rem; color: var(--color-text-muted); margin: 0 0 0.8rem; line-height: 1.5; }
  .hd-links { font-family: var(--font-mono); font-size: 0.8rem; letter-spacing: 0.01em; color: var(--color-text-muted); margin: 0; }
  .hd-links a { color: var(--color-accent); text-decoration: none; }
  .hd-links a:hover { text-decoration: underline; }
  .hd-links .sep { color: var(--color-text-ghost); margin: 0 0.45rem; }

  @media (max-width: 620px) {
    .hd { flex-direction: column; align-items: flex-start; gap: 1.4rem; }
    .hd-photo { order: -1; width: 168px; height: 210px; }
    .hd-name { font-size: 2rem; }
    .hd-tag { font-size: 1.05rem; }
  }

  /* ===== section frame ===== */
  .sec { margin-top: 3.6rem; }
  .sec-h {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--color-text-dim);
    margin: 0 0 1.4rem;
    padding-bottom: 0.6rem;
    border-bottom: 1px solid var(--color-border);
  }
  .sec-h::before { content: "// "; color: var(--color-accent); opacity: 0.6; }

  /* block heading + body (current work, research program) */
  .blk-h { font-family: var(--font-sans); font-size: 1.3rem; font-weight: 600; letter-spacing: -0.01em; color: var(--color-text); margin: 0 0 0.7rem; }
  .blk-h :global(em) { font-style: italic; }
  .blk-body { font-size: 0.98rem; line-height: 1.7; color: var(--color-text-muted); margin: 0 0 0.8rem; max-width: 46rem; }
  .blk-body :global(em) { font-style: italic; color: var(--color-text); }
  .blk-link { margin: 0; font-size: 0.9rem; }
  .blk-link a { color: var(--color-accent); text-decoration: none; }
  .blk-link a:hover { text-decoration: underline; }
  .block-note { font-size: 0.95rem; line-height: 1.65; color: var(--color-text-muted); margin: 0 0 1.4rem; }

  /* ===== timeline ===== */
  .tl { display: flex; flex-direction: column; gap: 2rem; }
  .tl-item { display: grid; grid-template-columns: 150px minmax(0, 1fr); gap: 1.75rem; }
  .tl-aside { display: flex; flex-direction: column; align-items: flex-start; gap: 0.6rem; }
  .tl-logo { min-height: 48px; display: flex; align-items: center; }
  .tl-logo img { height: 36px; width: auto; max-width: 124px; display: block; }
  .tl-logo.emblem img { height: 78px; }
  .tl-period { font-family: var(--font-mono); font-size: 11.5px; letter-spacing: 0.03em; color: var(--color-text-dim); }
  .tl-org { font-family: var(--font-sans); font-size: 1.08rem; font-weight: 600; color: var(--color-text); margin: 0 0 0.15rem; letter-spacing: -0.005em; }
  .tl-role { font-size: 0.92rem; color: var(--color-text-muted); margin: 0 0 0.7rem; }
  .tl-did { font-size: 0.95rem; line-height: 1.65; color: var(--color-text-muted); margin: 0 0 0.6rem; }
  .tl-did :global(em) { font-style: italic; }
  .tl-qs { list-style: none; margin: 0.55rem 0 0; padding: 0.1rem 0 0.15rem 0.95rem; border-left: 2px solid color-mix(in srgb, var(--color-accent) 30%, transparent); display: flex; flex-direction: column; gap: 0.4rem; }
  .tl-qs li { font-size: 0.92rem; line-height: 1.5; color: var(--color-text-muted); font-style: italic; }
  .tl-qs li :global(em) { color: var(--color-text); font-style: italic; }
  @media (max-width: 620px) {
    .tl-item { grid-template-columns: 1fr; gap: 0.9rem; }
    .tl-aside { flex-direction: row; align-items: center; gap: 0.9rem; }
  }

  /* ===== project / selected-work cards ===== */
  .proj-list { display: flex; flex-direction: column; gap: 1rem; min-width: 0; }
  .proj-card {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 300px;
    gap: 1.5rem;
    align-items: center;
    padding: 1.4rem 1.5rem;
    border: 1px solid var(--card-border);
    border-radius: 12px;
    background: var(--card-bg);
    text-decoration: none;
    color: inherit;
    transition: transform 0.2s ease, box-shadow 0.25s ease, border-color 0.2s ease;
  }
  .proj-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 14px 30px -14px rgba(23, 33, 48, 0.28);
    border-color: var(--card-hover-border);
  }
  .proj-card-current { border-color: color-mix(in srgb, var(--color-accent) 40%, var(--card-border)); }
  .proj-copy { min-width: 0; }
  .proj-top { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: 0.4rem; }
  .proj-title { font-family: var(--font-sans); font-size: 1.05rem; font-weight: 500; color: var(--color-text); transition: color 0.2s ease; }
  .proj-card:hover .proj-title { color: var(--color-accent); }
  .proj-title :global(em) { font-style: italic; }
  .proj-tag { flex: none; font-family: var(--font-mono); font-size: 10px; letter-spacing: 0.06em; text-transform: uppercase; color: var(--color-text-dim); white-space: nowrap; }
  .proj-card-current .proj-tag { color: var(--color-accent); }
  .proj-card-current .proj-tag::before { content: "\25CF\00a0"; font-size: 0.8em; }
  .proj-desc { font-size: 0.93rem; line-height: 1.65; color: var(--color-text-muted); margin: 0; }
  .proj-desc :global(em) { font-style: italic; }
  .proj-figure { min-width: 0; border-radius: 8px; overflow: hidden; }
  .proj-figure img { display: block; width: 100%; max-width: 100%; height: auto; aspect-ratio: 5 / 3; transition: transform 0.35s ease; }
  .proj-card:hover .proj-figure img { transform: scale(1.03); }

  /* ===== playground — playable cards ===== */
  .play-sec {
    --pg-blue: #2f58db;
    --pg-amber: #f6ad28;
    --pg-ink: #172137;
    --pg-cream: #f4efe3;
    --pg-indigo: #46567a;
  }
  .play-note { max-width: 46rem; }
  .play-note :global(em) { font-style: italic; color: var(--color-text); }

  .play-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.1rem; }
  .play-card {
    display: grid;
    grid-template-rows: 168px auto;
    border: 1px solid var(--card-border);
    border-radius: 14px;
    background: var(--card-bg);
    text-decoration: none;
    color: inherit;
    overflow: hidden;
    transition: transform 0.2s ease, box-shadow 0.25s ease, border-color 0.2s ease;
  }
  .play-card:hover { transform: translateY(-4px); }
  .play-relay:hover {
    border-color: color-mix(in srgb, var(--pg-blue) 45%, var(--card-border));
    box-shadow: 0 16px 34px -18px color-mix(in srgb, var(--pg-blue) 60%, transparent);
  }
  .play-im:hover {
    border-color: color-mix(in srgb, var(--pg-indigo) 50%, var(--card-border));
    box-shadow: 0 16px 34px -18px color-mix(in srgb, var(--pg-indigo) 50%, transparent);
  }

  .play-fig {
    position: relative;
    display: grid;
    place-items: center;
    border-bottom: 1px solid var(--card-border);
    overflow: hidden;
  }
  .play-relay .play-fig { background: radial-gradient(125% 125% at 50% 0%, #eef3ff 0%, #e3ebff 100%); }
  .play-im .play-fig { background: radial-gradient(125% 125% at 50% 0%, #f6f8fb 0%, #edf0f5 100%); }
  .play-fig svg { height: 150px; width: auto; display: block; }

  /* --- RELAY robot --- */
  .pr-ring { fill: none; stroke: var(--pg-ink); stroke-opacity: 0.16; stroke-width: 2; stroke-linecap: round; transform-origin: 110px 100px; }
  .pr-ring-a { stroke-dasharray: 2 8; animation: pr-spin 26s linear infinite; }
  .pr-ring-b { stroke-dasharray: 3 11; animation: pr-spin 34s linear infinite reverse; }
  .pr-disc { fill: var(--pg-cream); stroke: var(--pg-ink); stroke-opacity: 0.10; stroke-width: 1.5; }
  .pr-bot { transform: translate(46px, 34px); }
  .pr-stalk { fill: none; stroke: var(--pg-ink); stroke-width: 7; stroke-linecap: square; }
  .pr-bulb { fill: var(--pg-amber); stroke: var(--pg-ink); stroke-width: 5; transform-box: fill-box; transform-origin: center; animation: pr-glow 2.6s ease-in-out infinite; }
  .pr-ear { fill: var(--pg-amber); stroke: var(--pg-ink); stroke-width: 6; }
  .pr-head { fill: var(--pg-blue); stroke: var(--pg-ink); stroke-width: 7; }
  .pr-face { fill: var(--pg-cream); stroke: var(--pg-ink); stroke-width: 6; }
  .pr-eye { fill: #fff; stroke: var(--pg-ink); stroke-width: 4; }
  .pr-pupil { fill: var(--pg-ink); }
  .pr-eyes { transform-box: fill-box; transform-origin: center; }
  .pr-mouth { fill: none; stroke: var(--pg-ink); stroke-width: 5; stroke-linecap: round; }
  .play-relay:hover .pr-bot { animation: pr-bob 0.7s ease; }
  .play-relay:hover .pr-eyes { animation: pr-blink 0.45s ease; }
  .play-relay:hover .pr-bulb { animation: pr-glow-strong 0.9s ease-in-out infinite; }

  @keyframes pr-spin { to { transform: rotate(360deg); } }
  @keyframes pr-glow { 0%, 100% { filter: none; } 50% { filter: drop-shadow(0 0 4px rgba(246, 173, 40, 0.85)); } }
  @keyframes pr-glow-strong { 0%, 100% { filter: drop-shadow(0 0 3px rgba(246, 173, 40, 0.7)); } 50% { filter: drop-shadow(0 0 9px rgba(246, 173, 40, 1)); } }
  @keyframes pr-blink { 35%, 55% { transform: scaleY(0.12); } }
  @keyframes pr-bob { 0%, 100% { transform: translate(46px, 34px); } 50% { transform: translate(46px, 27px); } }

  /* --- Information Machines: an information state --- */
  .pg-edge { stroke: var(--pg-ink); stroke-opacity: 0.32; stroke-width: 2.5; stroke-linecap: round; }
  .pg-node { transform-box: fill-box; transform-origin: center; animation: pg-pulse 3.4s ease-in-out infinite; }
  .pg-q { animation-delay: 0.7s; }
  .pg-node circle { stroke: var(--pg-ink); stroke-width: 3.5; }
  .pg-p circle { fill: var(--pg-amber); }
  .pg-q circle { fill: var(--pg-blue); }
  .pg-label { font-family: var(--font-mono); font-size: 20px; font-weight: 600; text-anchor: middle; dominant-baseline: middle; }
  .pg-p .pg-label { fill: var(--pg-ink); }
  .pg-q .pg-label { fill: #fff; }
  .pg-spark { fill: var(--pg-blue); opacity: 0; animation: pg-travel 2.9s ease-in-out infinite; }
  .pg-halo { fill: none; stroke: var(--pg-indigo); stroke-width: 2.5; opacity: 0; animation: pg-reveal 5.4s ease-in-out infinite; }
  .pg-prime { font-family: var(--font-mono); font-size: 27px; font-weight: 600; fill: var(--pg-indigo); text-anchor: middle; opacity: 0; animation: pg-reveal 5.4s ease-in-out infinite; }

  @keyframes pg-pulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.06); } }
  @keyframes pg-travel {
    0% { transform: translate(0, 0); opacity: 0; }
    12% { opacity: 1; }
    68% { transform: translate(90px, -58px); opacity: 1; }
    74%, 100% { transform: translate(90px, -58px); opacity: 0; }
  }
  @keyframes pg-reveal { 0%, 56% { opacity: 0; } 68%, 86% { opacity: 1; } 100% { opacity: 0; } }

  /* --- card copy --- */
  .play-body { padding: 1.15rem 1.25rem 1.3rem; }
  .play-top { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: 0.4rem; }
  .play-title { font-family: var(--font-sans); font-size: 1.12rem; font-weight: 600; color: var(--color-text); letter-spacing: -0.005em; transition: color 0.2s ease; }
  .play-relay:hover .play-title { color: var(--pg-blue); }
  .play-im:hover .play-title { color: var(--pg-indigo); }
  .play-tag { flex: none; font-family: var(--font-mono); font-size: 10px; letter-spacing: 0.06em; text-transform: uppercase; color: var(--color-text-dim); white-space: nowrap; }
  .play-desc { font-size: 0.92rem; line-height: 1.65; color: var(--color-text-muted); margin: 0 0 0.95rem; }
  .play-chips { display: flex; flex-wrap: wrap; align-items: center; gap: 0.45rem; margin-bottom: 1.05rem; font-family: var(--font-mono); font-size: 10.5px; letter-spacing: 0.09em; text-transform: uppercase; color: var(--color-text-muted); }
  .play-chips-relay .play-chip:not(:first-child)::before { content: "\2192"; margin-right: 0.45rem; color: var(--pg-blue); }
  .play-chips-im .play-chip:not(:first-child)::before { content: "\00b7"; margin-right: 0.45rem; color: var(--pg-amber); font-weight: 700; }
  .play-cta { display: inline-flex; align-items: center; gap: 0.4rem; font-family: var(--font-mono); font-size: 11px; font-weight: 500; letter-spacing: 0.04em; text-transform: uppercase; }
  .play-relay .play-cta { color: var(--pg-blue); }
  .play-im .play-cta { color: var(--pg-indigo); }
  .play-cta-arrow { transition: transform 0.2s ease; }
  .play-card:hover .play-cta-arrow { transform: translateX(4px); }

  @media (max-width: 600px) {
    .play-grid { grid-template-columns: 1fr; }
  }
  @media (prefers-reduced-motion: reduce) {
    .pr-ring-a, .pr-ring-b, .pr-bulb, .pg-node, .pg-spark, .pg-halo, .pg-prime,
    .play-relay:hover .pr-bot, .play-relay:hover .pr-eyes, .play-relay:hover .pr-bulb { animation: none !important; }
    .pg-spark, .pg-halo, .pg-prime { opacity: 0; }
  }

  @media (max-width: 760px) {
    .proj-card { grid-template-columns: minmax(0, 1fr) 220px; gap: 1.15rem; }
  }
  @media (max-width: 560px) {
    .proj-card { grid-template-columns: 1fr; gap: 1rem; }
    .proj-card .proj-figure { order: -1; }
    .proj-top { flex-direction: column; align-items: flex-start; gap: 0.2rem; }
    .play-grid { grid-template-columns: 1fr; }
  }

  /* ===== writing (one featured note) ===== */
  .feat { display: block; text-decoration: none; padding: 0.1rem 0 0.2rem; }
  .feat-title { display: block; font-family: var(--font-sans); font-size: 1.05rem; font-weight: 500; color: var(--color-text); }
  .feat:hover .feat-title { color: var(--color-accent); }
  .feat-excerpt { display: block; margin-top: 0.35rem; font-size: 0.92rem; line-height: 1.6; color: var(--color-text-muted); max-width: 60ch; }
  .more { margin: 1.1rem 0 0; font-size: 0.9rem; }
  .more a { color: var(--color-accent); text-decoration: none; }
  .more a:hover { text-decoration: underline; }

  /* ===== contact cta ===== */
  .cta { border: 1px solid var(--color-border); border-radius: 12px; padding: 1.5rem 1.6rem; background: var(--color-surface); }
  .cta-line { margin: 0 0 0.5rem; color: var(--color-text); font-size: 0.98rem; line-height: 1.6; }
  .cta-links { margin: 0; font-size: 0.9rem; }
  .cta-links a { color: var(--color-accent); text-decoration: none; }
  .cta-links a:hover { text-decoration: underline; }
  .cta-links .sep { color: var(--color-text-ghost); margin: 0 0.45rem; }
</style>
